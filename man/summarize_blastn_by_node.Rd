% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_blastn_by_node.R
\name{summarize_blastn_by_node}
\alias{summarize_blastn_by_node}
\title{Summarize BLASTN pairwise similarity statistics for internal tree nodes}
\usage{
summarize_blastn_by_node(
  tree,
  blast,
  min_alignment_length = 400,
  pair_policy = c("all_hsps", "best_hsp"),
  diagnostics = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{tree}{A \code{treedata} or \code{phylo} object.}

\item{blast}{Either a path to a BLASTN outfmt 6 file, or a data.frame/tibble
containing the 12 standard BLAST outfmt 6 columns.}

\item{min_alignment_length}{Numeric scalar. Minimum alignment length retained.
Default: \code{400}.}

\item{pair_policy}{Character scalar. One of \code{"all_hsps"} or \code{"best_hsp"}.
Default: \code{"all_hsps"}.}

\item{diagnostics}{Logical. If \code{TRUE}, attach diagnostic information as an
attribute named \code{"diagnostics"}. Default: \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, print informative messages.
Default: \code{FALSE}.}
}
\value{
A tibble with one row per internal node and summary statistics of observed
BLASTN pairwise similarity among descendant taxa.

The returned tibble contains these columns:
\itemize{
\item \code{node}
\item \code{ntaxa}: number of descendant taxa in the node
\item \code{taxa}: descendant taxa labels
\item \code{n_blast_rows}: number of filtered BLASTN rows used for the node
\item \code{n_unique_pairs}: number of unique undirected taxa pairs represented by
those rows
\item \code{max_pident}, \code{max_pair_q}, \code{max_pair_s}, \code{max_length}, \code{max_gapopen}, \code{max_bitscore}
\item \code{min_pident}, \code{min_pair_q}, \code{min_pair_s}, \code{min_length}, \code{min_gapopen}, \code{min_bitscore}
}
}
\description{
Given a phylogenetic tree and BLASTN outfmt 6 results, summarize observed
within-node pairwise identity statistics for descendant taxa at each internal
node.

By default, every filtered BLASTN row/HSP whose query and subject are both
descendant taxa of a node is considered in that node's min/max summary. If
\code{pair_policy = "best_hsp"}, each undirected taxa pair is first collapsed to
its best HSP using the ranking: highest \code{pident}, then longest alignment,
then highest \code{bitscore}.
}
\details{
Self-hits and rows with missing query IDs, subject IDs, alignment length, or
percent identity are removed. The function summarizes observed BLAST pairs;
it does not impute missing pairwise comparisons.
}
\examples{
\dontrun{
tre <- treeio::read.iqtree("example.treefile")
res <- summarize_blastn_by_node(
  tree = tre,
  blast = "blast.outfmt6"
)
}

}
