% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_rank_monophyly.R
\name{check_rank_monophyly}
\alias{check_rank_monophyly}
\title{Check monophyly of taxa at a given metadata rank on a rooted tree}
\usage{
check_rank_monophyly(
  tree,
  data,
  rank_col,
  tip_col = "genome_label",
  taxa = NULL,
  min_tips = 2L,
  near_mono_frac = 0.8,
  polyphyly_second_frac = 0.3,
  polyphyly_largest_max = 0.6,
  drop_na = TRUE,
  check_rooted = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{tree}{A rooted \code{phylo} object.}

\item{data}{A data.frame or tibble containing tip metadata.}

\item{rank_col}{Character scalar. Column name in \code{data} giving the taxonomic
rank to evaluate.}

\item{tip_col}{Character scalar. Column in \code{data} containing tip labels
matching \code{tree$tip.label}. Default: \code{"genome_label"}.}

\item{taxa}{Optional character vector of taxa to evaluate. Default \code{NULL}
means all taxa present at \code{rank_col}.}

\item{min_tips}{Minimum number of tips required to assess a taxon.
Default: \code{2L}.}

\item{near_mono_frac}{Minimum fraction of members in the largest pure cluster
to classify a taxon as \code{near_monophyletic_minor_outliers}. Default: \code{0.80}.}

\item{polyphyly_second_frac}{Minimum fraction of members in the second-largest
pure cluster to classify a taxon as \code{polyphyletic}. Default: \code{0.30}.}

\item{polyphyly_largest_max}{Maximum fraction of members in the largest pure
cluster for a taxon to be classified as \code{polyphyletic}. Default: \code{0.60}.}

\item{drop_na}{Logical. If \code{TRUE}, taxa with missing values in \code{rank_col}
are ignored. Default: \code{TRUE}.}

\item{check_rooted}{Logical. If \code{TRUE}, error when \code{tree} is not rooted.
Default: \code{TRUE}.}

\item{verbose}{Logical. If \code{TRUE}, print progress messages.
Default: \code{FALSE}.}
}
\value{
A tibble with one row per taxon and these columns:
\itemize{
\item \code{rank}
\item \code{taxon}
\item \code{n_tips}
\item \code{status}
\item \code{largest_pure_cluster_n}
\item \code{largest_pure_cluster_frac}
\item \code{second_pure_cluster_n}
\item \code{second_pure_cluster_frac}
\item \code{n_pure_clusters}
\item \code{n_outside_largest_cluster}
\item \code{mrca_clade_size}
\item \code{mrca_contaminant_tips}
\item \code{mrca_purity}
\item \code{interpretation}
}
}
\description{
Assess whether taxa at a specified metadata rank (for example: phylum, class,
order, family, genus, species) are monophyletic on a rooted phylogenetic tree.

The function aligns \code{data} to \code{tree$tip.label} using \code{tip_col}, then evaluates
each taxon at the requested \code{rank_col}.

Classification logic:
\itemize{
\item \code{monophyletic}: all members form one pure clade
\item \code{near_monophyletic_minor_outliers}: one dominant cluster plus only a few
stray members elsewhere
\item \code{polyphyletic}: the taxon is split into multiple substantial clusters
\item \code{non_monophyletic_split}: non-monophyletic, but not strongly polyphyletic
\item \code{singleton} / \code{too_few_tips}: too few members to assess robustly
}
}
\examples{
# res_order <- check_rank_monophyly(
#   tree = tre,
#   data = metadata,
#   rank_col = "order",
#   tip_col = "genome_label"
# )
#
# dplyr::count(res_order, status)
# dplyr::filter(res_order, status != "monophyletic")

}
